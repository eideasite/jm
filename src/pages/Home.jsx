import React from 'react';
import AppContent from '../components/layout/AppContent'; // your existing content component

const Home = ({ darkMode }) => {
  return <AppContent darkMode={darkMode} />;
};

export default Home;
